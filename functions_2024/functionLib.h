#pragma once
#include <iostream>
#include <vector>

//function forward declarations
int genRandomValueInRange(int min, int max);
void fillRandomArray(std::vector<int> &ourArray, int arrLength, int arrMin, int arrMax);
void printArray(std::vector<int> ourArray);